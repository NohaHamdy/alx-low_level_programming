#include "main.h"
/**
 *add - a function that adds two integers and returns the result
 *@a: first number
 *@b:second number
 *Return: to return the result
 */
int add(int a, int b)
{
	int r;

	r = a + b;
	return (r);
}
