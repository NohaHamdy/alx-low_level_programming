#include "main.h"
/**
 *mul - a function that multiplies two integers
 *@a: first number
 *@b:second number
 *Return: to return the result
 */
int mul(int a, int b)
{
	int r;

	r = a * b;
	return (r);
}
